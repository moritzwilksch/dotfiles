# yaml-language-server: $schema=https://raw.githubusercontent.com/jesseduffield/lazygit/refs/heads/master/schema/config.json

git:
  pagers:
    - externalDiffCommand: difft --color=always

customCommands:
  - key: "<c-g>"
    context: "files"
    description: "Autogenerate commit message from staged diff (Gemini)"
    command: 'git commit -m {{.Form.Message | quote}}'
    loadingText: "Generating commit message..."
    prompts:
      - type: "input"
        title: "Commit message:"
        key: "Message"
        initialValue: "{{ runCommand `sh -c '\"$HOME/Library/Application Support/lazygit/gen-commit-msg.py\"'` }}"
