#!/bin/bash
{{ if eq .chezmoi.os "windows" -}}
# Skip pixi install on Windows
exit 0
{{ end -}}

if command -v pixi >/dev/null 2>&1; then
    echo "pixi is already installed"
    exit 0
fi

echo "Installing pixi..."
curl -fsSL https://pixi.sh/install.sh | bash
